services:
  db:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=la-chaise-du-savoir
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - lcds-network

  back:
    #build: .
    image: ghcr.io/quentintnl/la-chaise-du-savoir:latest@sha256:78b9372a6188d37f3905cf4317cd70f0c22932ad8da3dab64a83457076f02c22
    ports:
      - "8080:8080"
    environment:
      - "SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/la-chaise-du-savoir"
    networks:
      - lcds-network

  front:
    build: ../la-chaise-du-savoir-front
    ports:
      - "3000:3000"
    environment:
        - "NEXT_PUBLIC_API_BASE_URL=http://back:8080/api"
    networks:
      - lcds-network

networks:
  lcds-network:
    name: lcds-network
    driver: bridge

volumes:
  db_data:
